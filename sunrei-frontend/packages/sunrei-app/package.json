{
  "name": "sunrei-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "clean": "rm -rf .next node_modules",
    "codegen": "rm -rf src/dto && openapi-generator-cli generate -i ../../sunrei-api/app-api-spec.yaml -g typescript-axios -o src/dto --additional-properties=typescriptThreePlus=true --global-property=apiDocs=false,modelDocs=false"
  },
  "dependencies": {
    "@tanstack/react-query-devtools": "^5.90.2"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.20.2",
    "@types/google.maps": "^3.58.1"
  }
}
